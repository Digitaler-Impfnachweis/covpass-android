package com.ibm.health.common.vaccination.app.dialog

/**
 * Enum to distinguish between three possible dialog buttons / actions.
 */
public enum class DialogAction {
    POSITIVE, NEUTRAL, NEGATIVE
}
